<%= form_for @ingredient do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :name %>
  <%= f.collection_select :smoothie_id, Smoothie.all, :id, :name %>

  <%=  f.submit %>

  <% end %>
